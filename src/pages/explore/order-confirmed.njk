---
meta:
  lang: en
  title: order confirmation page
  description: eric tuovila's personal site
layout: base
eleventyNavigation:
  key: order confirmed
  hideInMainNav: true
---

<div class="row align-items-md-stretch">
  <!--- Contentsquare --->
  <div class="col-md-4 mt-3">
    <div class="p-4 pb-5 text-dark rounded-3 bg-light">
      <h4 class="pt-2 pb-2" style="border-bottom: 2px solid darkblue;">Contentsquare</h4>
      <div class="mt-4">
      <!--- CS Errors --->
      <h5 class="p-2">⚠️ Errors</h5>
      <div class="m-3 pt-2 pb-2">
        <button class="btn btn-outline-dark" onclick="setError('js')">JS Error</button>
        <button class="btn btn-outline-dark" onclick="setError('api')">API Error</button>
        <button class="btn btn-outline-dark" onclick="setError('custom')">Custom Error</button>
      </div>
      <!--- CS Session Replay --->
      <h5 class="p-2">🎥 Session Replay</h5>
      <div class="m-3 pt-2 pb-2">
        <button class="btn btn-outline-dark" onclick="triggerRecording('ETP')">ETR (Page) event</button>
        <button class="btn btn-outline-dark align-right" onclick="triggerRecording('ETS')">ETR (Session) event</button>
        <br/><br/>
        <input id="excludePage-input" type="text" placeholder="Exclude regex here" title="Enter Session Replay exclude regex pattern"/>
        <button class="btn btn-sm btn-outline-dark" onclick="excludeCsRecording(document.getElementById('excludePage-input').value)">Set Replay Exclusion Rule</button>
        <br/><br/>
        <input id="encryptEls-input" type="text" placeholder="CSS selectors here" title="Enter Controlled Exposure element CSS selectors"/>
        <button class="btn btn-sm btn-outline-dark" onclick="setEncryptedCaptures(document.getElementById('encryptEls-input').value)">Set Controlled Exposure Rule</button>
      </div>
    </div>
    </div>
  </div>
</div>
<!-- Modal -->
      <div class="modal fade" id="infoModal" tabindex="-1" aria-labelledby="infoModalLabel" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title" id="infoModalLabel">Placeholder title</h5>
            </div>
              <div class="mb-4 bg-light rounded-3">
                <div class="container-fluid py-2">
                  <div class="modal-body">Placeholder content</div>
                </div>
              </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" aria-label="Close">Close</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
<div class="row mt-3">
</div>
